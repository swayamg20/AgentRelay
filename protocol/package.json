{
	"name": "@agentrelay/protocol",
	"version": "0.1.0",
	"description": "Executable protocol contracts for AgentRelay Missions, deliveries, and runtime adapters.",
	"license": "MIT",
	"type": "module",
	"engines": {
		"node": ">=20.0.0"
	},
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		},
		"./testing": {
			"types": "./dist/testing/index.d.ts",
			"import": "./dist/testing/index.js"
		}
	},
	"files": ["dist", "fixtures"],
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"prepack": "pnpm run build",
		"test": "vitest run",
		"test:exports": "node --input-type=module -e \"const p = await import('@agentrelay/protocol'); const t = await import('@agentrelay/protocol/testing'); if (!p.missionManifestSchema || !p.transitionDeliveryState || !p.acceptHostEvent || !p.hostExecutionAttemptSchema || !p.MAX_HOST_PEER_MESSAGES || !p.missionCoordinatorEventSchema || !p.missionCoordinatorAppendInputSchema || !p.missionCoordinatorStateSchema || !p.missionCreationResultSchema || !p.missionParticipantAcceptanceInputSchema || !p.missionParticipantAcceptanceResultSchema || !p.nodeMissionAssignmentSchema || !p.nodeMissionAssignmentResultSchema || !p.nodeMissionAssignmentListRequestSchema || !p.nodeMissionAssignmentListSchema || !p.storedMissionDeliveryItemSchema || !p.storedMissionDeliveryCursorPageSchema || !p.recoverableMissionDeliveryPageSchema || !p.deliveryClaimInputSchema || !p.deliveryStartInputSchema || !p.deliveryRenewInputSchema || !p.deliveryCompleteInputSchema || !p.deliveryReleaseInputSchema || !p.deliveryClaimResultSchema || !p.deliveryStartResultSchema || !p.deliveryRenewResultSchema || !p.deliveryCompleteResultSchema || !p.deliveryReleaseResultSchema || !p.deliveryOperationReceiptSchema || !p.deliveryCancellationResultSchema || !p.nodeEnrollmentInputSchema || !p.nodeCredentialRotationInputSchema || !p.nodeDescriptorSchema || !p.ownedNodeSummarySchema || !p.issuedNodeCredentialSchema || !p.nodeEnrollmentResultSchema || !p.nodeCredentialRotationResultSchema || !p.nodeSelfResultSchema || !p.ownedNodeListSchema || !p.workspaceRegistrationInputSchema || !p.workspaceBindingDescriptorSchema || !p.workspaceRegistrationResultSchema || !p.workspaceBindingListSchema || !p.storedDeliveryCursorPageSchema || !t.FakeAgentHostAdapter || !t.runMissionFixture || !t.backendAndroidMissionFixture) throw new Error('protocol exports missing')\"",
		"test:watch": "vitest",
		"typecheck": "tsc -p tsconfig.json --noEmit"
	},
	"dependencies": {
		"zod": "^4.4.3"
	},
	"devDependencies": {
		"@types/node": "^22.10.0",
		"typescript": "^5.7.2",
		"vitest": "3.2.6"
	}
}
